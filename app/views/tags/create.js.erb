$( '.dropdown' ).remove();

var $name = $( "<div>", { class: 'name', text: '<%= @character.name %>' } );
var $frame = $( '.frame' ).last();
$frame.append( $name );
$frame.attr( 'id', '<%= @character.name %>');
